{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow — transactional task manager (IT0043 lab)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3000",
    "serve": "vite",
    "start": "json-server db.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",
    "json-server": "^0.17.4",
    "serve": "^14.2.4",
    "vite": "^6.3.5"
  }
}
